package frc.sciborgs.scilib;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ThingTest {
    @Test void someLibraryMethodReturnsTrue() {
        Thing thing = new Thing();
        assertEquals(thing.getNumber(), 0.2);
    }
}
